name: CI
on: [push] # This trigger ensures it runs when you 'git push'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run a one-line script
        run: echo Hello, GitHub Actions!